include incl.pipl      # george from here

model = .mesh .texture : !$.blend sh process_blend.sh !$ !0
font  = .font .texture : !$.ttf   sh process_font.sh !$

model cube flat

model sphere smooth
george misty ass two
font bembo
